{% extends "base.html.twig" %}

{% block title %}
    Accueil 
          
        
      
    
{% endblock %}

{% block body %}
    {% if app.user %}
    <div class="mb-3">
        vous etes connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">deconnection</a>
    </div>
{% endif %}
    <h1>
        Bienvenue sur la page d'accueil. Visualisez vos films par genre, studio et acteur présents dans la bibliothèque !
              
          
    </h1>

    
          <div class="row">
    {# {% for user in users %} #}
{# <p> {{ user}} </p>
    {% endfor %}} #}
      
        {% if movies is defined %}
        <div class="col-2">
            <h5>
                Movie
            </h5>
            <ul class="list-group">
                {% for movie in movies %}
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                         {{movie.id}}
                         {{movie.name}}
                        
                         
                            
                        </a>
                        <span class="badge bg-primary rounded-pill">
                         
                        </span>
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
      {% if studios is defined %}
        <div class="col-2">
            <h5>
                Studio
            </h5>
            <ul class="list-group">
                {% for studio in studios %}
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                       {{ studio.id }}
                       {{studio.name}}
                      
                            
                        </a>
                        <span class="badge bg-primary rounded-pill">
                         
                        </span>
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
      {% if genres is defined %}
        <div class="col-2">
            <h5>
                Genre
            </h5>
            <ul class="list-group">
                {% for genre in genres %}
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                       {{ genre.id }}
                       {{genre.name}}
                      
                            
                        </a>
                        <span class="badge bg-primary rounded-pill">
                         
                        </span>
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
    {% if actors is defined %}
        <div class="col-2">
            <h5>
                Actors
            </h5>
            <ul class="list-group">
                {% for actor in actors %}
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                       {{ actor.id }}
                       {{actor.firstname}}
                       {{actor.lastname}}

                      
                            
                        </a>
                        <span class="badge bg-primary rounded-pill">
                         
                        </span>
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
    
{% endblock %}